---
title: 11.13 Nature d'un champ (autocomplete)
robots: noindex
sidebar_position: 13
---

import IconedLink from '@site/src/components/IconedLink';
import {CriteriaAA} from '@site/src/components/CriteriaTag';
import { BookmarkletTags } from '@site/src/components/ToolsLinks';
import Example from "@site/src/components/Example";
import NCChaffoteaux from './assets/11.13-nc-chaffoteaux.png';
import CAccess42 from './assets/11.13-c-access42.png';

# La finalité d’un champ de saisie peut-elle être déduite pour faciliter le remplissage automatique des champs avec les données de l’utilisateur ?

<CriteriaAA/>
<IconedLink>[Lien RGAA](https://accessibilite.numerique.gouv.fr/methode/criteres-et-tests/#11.13)</IconedLink>
<BookmarkletTags names={["11.13 Autocomplete"]} />

## Que vérifie-t-on ?

Pour les champs qui collectent des informations personnelles sur l’utilisateur, la finalité du champ est
déterminable à partir du code.

On recherche la présence de la propriété `autocomplete`.

**Ressources :**
- [WCAG - Finalités de saisie des composants d’interface utilisateur](https://www.w3.org/Translations/WCAG21-fr/#input-purposes)
- [RGAA - liste des valeurs possibles pour l’attribut `autocomplete`](https://accessibilite.numerique.gouv.fr/methode/glossaire/#liste-des-valeurs-possibles-pour-l-attribut-autocomplete)
- [WCAG - liste des valeurs possibles (anglais)](https://www.w3.org/TR/html52/sec-forms.html#autofill-processing-model "WCAG liste des valeurs possibles - en anglais - nouvelle fenêtre")

<details>
    <summary>
        ## Exemples
    </summary>

    <Example imgImport={NCChaffoteaux}>
        Sur le site chaffoteaux.fr, plusieurs champs attendent une donnée de l’utilisateur (nom, prénom, email, téléphone),
        mais aucun n’a de propriété `autocomplete`.

        Pour corriger on pourra recommander l’ajout de la propriété `autocomplete` sur les champs nom, prénom,
        email et téléphone, avec les valeurs respectives `family-name`, `given-name`, `email`, `tel-national`.
    </Example>

    <Example imgImport={CAccess42} comply>
        Sur le site [access42.net](https://access42.net/#contact), les champs de données personnelles sont
        correctement identifiés avec la propriété `autocomplete`.
    </Example>
</details>

## Aide à la détection
<BookmarkletTags names={["11.13 Autocomplete"]} />

- Repérer dans la page les champs qui attendent des données personnelles.
- Vérifier la présence d’une propriété `autocomplete`.
- Grâce au dictionnaire positionné sur la gauche, vérifier la validité de la valeur affichée et sa pertinence.

## Notes techniques

La [liste des valeurs possibles pour l’attribut `autocomplete`](https://accessibilite.numerique.gouv.fr/methode/glossaire/#liste-des-valeurs-possibles-pour-l-attribut-autocomplete)
repose sur la liste des valeurs présentes dans la spécification WCAG2.1 qui reprend elle-même la liste des valeurs
de type “field name” de la spécification HTML5.2. Le critère WCAG demande à ce que l’une de ces valeurs soit
présente pour qualifier un champ de saisie concernant l’utilisateur.

Ce que le critère WCAG laisse implicite, ce sont les différentes règles de construction possibles pour obtenir
une valeur (simple ou composée) pour l’attribut `autocomplete`. C’est cependant l’affaire du développeur de
fournir à l’attribut `autocomplete` une valeur ou un ensemble de valeurs valides au regard des exigences de
l’algorithme fourni par la spécification HTML5.2. Ainsi, un attribut `autocomplete` ne peut contenir
qu’une seule valeur de type `“field name”`, comme `"name"` ou `"street-address"`. On peut avoir également
un ensemble composé de différentes valeurs comme, par exemple, `autocomplete="shipping name"`
ou `autocomplete="section-software shipping street-address"` : `"section-software"` renvoie à une valeur
de type “scope” et `"shipping"` à une valeur de type “hint set”, mais toujours une seule valeur de type “field name”.

## Références

### WCAG 2.1

#### Critère(s) de succès :

*   [1.3.5 (AA)](https://www.w3.org/Translations/WCAG21-fr/#identify-input-purpose "critère 1.3.5 (AA) - nouvelle fenêtre")

#### Technique(s) suffisante(s) et/ou échec(s) (en anglais) :

*   [H98](https://www.w3.org/WAI/WCAG21/Techniques/html/H98 "H98 - nouvelle fenêtre")

### EN 301 549 V2.1.2 (2018-08) (en anglais)

*   9.1.3.5 Identify Input Purpose (AA)